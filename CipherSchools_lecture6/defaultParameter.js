const addTwoNos = (a, b =57) => a+b ;
console.log(addTwoNos(23));

// when 1 parameter is passed then the value of default parameter is used
// if we do not assign default value to one parameter and one value is passed then ouput is observed as "NaN"